import React from 'react'

const Gallery = () => {
    return (
        <section className="w-screen h-screen bg-green-500">
            <h2>Gallery section in grid format (4 columns, 2 rows)</h2>
        </section>
    )
}

export default Gallery